
  {{#each comments as |comment| }}
  <div class="comments">

  <section class="comment">
    <div class="meta">
      {{user.name}} on
      {{format_date date_created}}
    </div>
    <div class="text">
      {{comment.comment_body}}
    </div>
  </section>

</div>
  {{/each}}