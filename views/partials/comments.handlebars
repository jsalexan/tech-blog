  {{#each this }}
  <div class="comments">

  <section class="comment">
    <div class="meta">
      {{user.name}} 
    </div>
    <div class="text">
      {{comment_body}}
    </div>
  </section>

</div>
  {{/each}}

