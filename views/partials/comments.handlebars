
{{#each comments as |comment| }}
  <div class="comments">

  <section class="comment" id="comment">
    <div class="meta">
       <p> Posted by
      {{user.name}} 

        </p>
    </div>
    <div class="text">
      {{comment_body}}
    </div>
  </section>

</div>
  {{/each}}