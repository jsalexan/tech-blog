
  {{#each this}}
  <div class="comments">

  <section class="comment">
    <div class="meta">
      {{user.name}} on
      {{!-- {{format_date post.date_created}} --}}
    </div>
    <div class="text">
      {{comment_body}}
    </div>
  </section>

</div>
  {{/each}}